# THE LEDE

First sentence for people starting a business, plus a cream rack that prints other live websites. REDLINE first-draft. Client-owned static HTML.

Drop `index.html` at the root of a Vercel project. No build step.
